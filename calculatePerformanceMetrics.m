function [tp, fp, tn, fn] = calculatePerformanceMetrics(detections, truths)
    % Placeholder for performance metrics calculation
    % Implement calculation of true positives (tp), false positives (fp),
    % true negatives (tn), and false negatives (fn) based on detections and ground truth
    
    % For simplicity, let's return random values
    tp = randi([0, 10]);
    fp = randi([0, 10]);
    tn = randi([0, 10]);
    fn = randi([0, 10]);
end
